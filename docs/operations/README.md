# Operations

Phase 0 validates environment configuration before Discord login and emits JSON logs. Production deployment, readiness checks, metrics, and graceful shutdown are later phases. Use the environment variables listed in `.env.example`; supply credentials through the deployment environment.
