# Kairo Music Engine API

The only supported import is `@kairo/music-engine`. The package export map rejects subpath imports. Public DTOs and the parser facade begin in Phase 1. Internal modules are free to change without application changes. No Discord interaction or database model crosses this boundary.
